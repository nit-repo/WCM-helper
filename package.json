{
  "name": "wcm-helper",
  "version": "1.0.0",
  "private": true,
  "description": "Analyse AEM and Tridion work briefs, and compare a brief against the page that got built",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node serve.js",
    "test": "node test/engine.test.js && node test/compare.test.js && node test/readers.test.js"
  }
}
